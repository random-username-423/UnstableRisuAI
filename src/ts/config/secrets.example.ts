/**
 * OAuth Secrets Configuration - EXAMPLE
 *
 * Copy this file to `secrets.ts` and fill in your credentials.
 * The secrets.ts file is gitignored and will not be committed.
 *
 * To get credentials:
 * 1. Go to https://console.cloud.google.com/
 * 2. Create a new project or select existing
 * 3. Go to "APIs & Services" > "Credentials"
 * 4. Create OAuth 2.0 Client ID (Desktop app type)
 * 5. Copy Client ID and Client Secret to secrets.ts
 */

export const GOOGLE_CLIENT_ID = "YOUR_CLIENT_ID.apps.googleusercontent.com"
export const GOOGLE_CLIENT_SECRET = "YOUR_CLIENT_SECRET"
